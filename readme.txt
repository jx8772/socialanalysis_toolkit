to Execute the program:

java -cp "./out/artifacts/socialanalysis_toolkit_jar/socialanalysis_toolkit.jar" collaborativefiltering.Prediction  ./config/Prediction/properties

To sync with github

git add -A
git commit -m "comment" -a
git push origin master

